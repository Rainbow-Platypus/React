export const initialState = {
    description : ''
}

export function reducer(state, action){
    switch (action.type) {
        case 'TEST':
        return {...state};

        case 'TEST2':
        return {...state};

    }
}